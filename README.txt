auth0

is a example auth0.

Prerequisites:
- .NET SDK 5.0


=====================================
Development Environment
=====================================

auth0 application:
- On any terminal move to the "weatherforecast" folder (the folder containing the "weatherforecast.csproj" file) and execute these commands:

dotnet restore
dotnet build
dotnet run